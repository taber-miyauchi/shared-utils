[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "shared-utils"
version = "1.0.0"
description = "Shared utilities library for cross-repo SCIP demo"
requires-python = ">=3.10"
